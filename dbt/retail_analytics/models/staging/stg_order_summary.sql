select
    order_id,
    customer_id,
    order_status,
    order_ts,
    order_date,
    loyalty_tier,
    city,
    state,
    country,
    order_item_count,
    total_quantity,
    items_total_amount,
    invalid_line_count,
    payment_method,
    payment_status,
    payment_amount,
    paid_at,
    carrier,
    shipment_status,
    shipped_at,
    delivered_at,
    source_ingestion_date,
    source_run_id,
    curated_run_id,
    processed_at_utc,
    warehouse_loaded_at_utc
from {{ source('warehouse_staging', 'order_summary') }}
