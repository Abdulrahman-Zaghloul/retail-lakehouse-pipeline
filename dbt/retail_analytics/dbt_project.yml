name: retail_analytics
version: "1.0.0"
config-version: 2

profile: retail_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  retail_analytics:
    staging:
      +schema: staging_dbt
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table
